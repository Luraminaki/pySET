# Copy this file to `.env` (which is gitignored) and fill in real values.
# Anything set here, or in the real process environment, overrides config.json.

# Secret required to authorize the admin `delete_running_games` endpoint.
# Leave unset (or empty) to disable that endpoint entirely.
PYSET_ADMIN_SECRET=
